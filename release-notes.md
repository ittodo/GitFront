# GitFront Preview 0.0.3

- Remove the intentional 250 ms delay before loading branches that contain a commit.
- Keep duplicate hover lookups suppressed while branch information is being loaded.
- Ignore a completed lookup when its virtualized commit row has already been reused.
